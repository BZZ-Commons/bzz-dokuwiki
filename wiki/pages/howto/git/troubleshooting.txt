====== Problem eingrenzen ======
Jetzt muss ich wissen, welches Problem aufgetreten ist. 

Lies jede Aussage genau durch und vergleiche die Angaben mit deinem Projekt. Klicke den Button für die erste Aussage die auf dich zutrifft.

==== Log In to GitHub ====
Beim Klonen oder beim Push erscheint ein Fenster "Log In To GitHub": => //**[Authentifizieren]**//

Das Fenster "Log In To GitHub" erscheint nicht, aber das Klonen scheitert. => //**[Authentifizieren]**//

{{:howto:git:classroom_student_51.png?400|}}

==== Ordner "venv" ====

In meinem Projekt gibt es keinen Ordner "venv" => **//[venv]//**

{{:howto:git:classroom_student_15.png?400|}}

==== No Python interpreter ====

(SCREENSHOT)

Ich erhalte die Meldung "No Python interpreter configured"  => //**[Interpreter]**//

==== (venv) im Terminal ====

{{:howto:git:classroom_student_12.png?600|}}

Wenn ich den Reiter "Terminal" öffne, fehlt am Anfang die Anzeige (venv)  => //**[Venv aktivieren]**//